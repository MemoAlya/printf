#ifndef MY_PRINTF_H
#define MY_PRINTF_H
#include <stdarg.h>
#include <stdio.h>

/**
 * _printf - a function that produces output according to a format
 */

int main(void);
/**
 * int _printf(const char *format, ...);
 */
#endif
